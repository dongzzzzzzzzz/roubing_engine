# roubing_engine 方案一致性审查：2026-03-02

审查日期：2026-09-14

审查范围：

- `roubing_ai_driven_stock_selection_engineering_v1.md`
- `ROUBING_AI_EXECUTABLE_REASONING_MANUAL_V1.md`
- 当前 `roubing_engine` 的 Stage B/C、任务解析器、事实包、规则检索、计划编译和审计链路
- `runs/20260302` 的真实盘后推演产物

本审查不读取、不恢复 `runs/_deferred_20260303` 中的次日数据。

## 一、结论

当前项目完成了方案的外层工程框架，但核心决策链没有完全按方案实现。

已经存在的正确外层包括：

- 先做盘后 Stage B/C，再做次日 Stage D；
- 事实、规则、AI 推断和独立审计分层；
- 盘后冻结 PRIMARY/BACKUP，盘中不临时加票；
- 缺分钟线、逐笔和板块响应时拒绝确认相关结论；
- 计划未通过审计时不落正式账本、不进入次日验证。

但最关键的内层顺序被改写了：

```text
方案要求：
市场环境 → 方向关系 → 当前节点 → 节点打开候选池 → 功能角色
→ 真实竞争组 → 当前最需要完成的任务 → 次日条件计划

当前实际：
市场环境 → 用固定顺序挑一个方向 → 扫描该方向的板位结构
→ 自动生成最高板/同板级/首板/炸板任务 → 从中挑最容易缩成一两只的任务
→ 再给这个任务补节点、角色和次日解释
```

所以 2026-03-02 出现的问题不是一次模型偶发失误，而是 Prompt、任务解析器和硬校验共同允许的系统性偏差。独立审计最后拦住了错误计划，说明最后一道门有效；但前面的程序门没有真正检查 roubing 的因果顺序。

## 二、3 月 2 日为什么会选成“商业航天—泰嘉股份”

### 1. Stage B 先把“高度”变成不可逆优先级

当日主要方向事实：

- 商业航天：1 只 3 板、4 只首板、2 只炸板；昨日同名样本一正一负；缺 2 板层。
- 电网设备：2 只 2 板、1 只首板、0 炸板；昨日 4 个样本全部正反馈，其中 2 只继续涨停。
- 石油化工：1 只 2 板、5 只首板、2 只炸板。
- 油服工程：9 只首板、0 炸板，但没有晋级层。

方案要求同时检查结构、角色、昨日买方反馈、分歧修复、板块响应、资金关系、催化和风险，不能由单项指标直接决定主路径。

当前 `prompts.py` 却写成：先比较高度、晋级层、首板和成交；前一层拉开后，后面的昨日反馈、修复、监管和催化不能反转。这使“3 板高于 2 板”成为事实上的固定优先级。

因此，商业航天即使存在中位断层、炸板和混合买方反馈，也被固定在电网设备之前。

### 2. Stage B 实际没有打开商业航天动作生成器

Stage B 对商业航天只建立：

```text
node_type = MAINSTREAM_CONFIRMATION_OBSERVATION
generator = null
candidate_ids = []
```

它自己也明确写了：板块指数、先锋、容量和完整扩散尚未共同确认，不能强开单票生成器。

按照方案，这一步之后应得到：

```text
商业航天继续观察，但当前没有由合法节点打开的动作池。
```

若没有其他合法动作节点，盘后应输出不行动，而不是继续找一只票。

### 3. `task_resolver.py` 绕过节点，自动制造四类 READY 任务

当前解析器只要看到主路径中有涨停或炸板股票，就自动生成：

- 最高身位延续；
- 同板级晋级；
- 首板扩散；
- 炸板修复。

这些任务不要求 Stage B 的节点生成器已经打开。候选池里只要有股票，状态就会成为 `READY`。

所以商业航天虽然 `generator=null`，仍被生成四个可供 Stage C 选择的执行任务。其中“最高身位延续”只有泰嘉股份一只动作股票。

### 4. Stage C 因为该任务只有一只票而选择它

Stage C 的真实选择理由明确包含：

```text
动作池只有一个确定对象，可收敛为 SINGLE；
其他任务有 4 只或 2 只，较难收敛。
```

这是倒果为因。正确顺序应是：

```text
先判断当前节点最需要谁完成什么任务
→ 再打开这一任务的完整候选池
→ 最后看能否收敛；不能收敛就不行动
```

不能因为某个池股票少，就反过来认定它是明天最重要的任务。

### 5. 不同任务的低位股票又被错误写成“替代最高身位”

四只首板只承担低位扩散和方向响应验证，泰嘉股份承担最高身位延续。它们不是同一功能角色，也不在同一动作竞争组。

低位股票先强、泰嘉随后上涨，最多说明泰嘉的独立主动性没有成立，或泰嘉被低位反推。它不能自动证明低位股票已经接管最高身位角色。

真正的角色替代至少要求：

- 新旧对象承担同一任务或已明确发生功能迁移；
- 旧角色先失效；
- 新对象独立增强；
- 板块或相关层级响应；
- 后续继续维持新角色。

当前任务解析器和 Stage C 文案把“被反推”“替代压力”“角色替代”混在了一起。

## 三、方案逐阶段对照

| 方案阶段 | 方案要求 | 当前实现 | 结论 |
| --- | --- | --- | --- |
| 信息边界 | T 日盘后不能读取 T+1 | 3 月 3 日数据已移出正式仓库 | 基本符合 |
| 市场环境 | 指数、广度、昨日反馈、亏钱效应和前态共同判断 | 有事实包，但部分已有事实没有 fact_id；历史状态可被非相邻旧账本污染 | 部分符合 |
| 主流方向 | 比较完整结构和资金关系，不使用固定分数或单项排名 | Prompt 把高度等字段写成固定、不可逆的先后顺序 | 不符合核心逻辑 |
| 当前节点 | 找到旧规律被打破、转折或迁移的事件 | Stage B 能输出节点，但缺少严格节点状态和动作资格 | 部分符合 |
| 节点开池 | 只有合法节点才能打开对应生成器 | `task_resolver` 可在 `generator=null` 时自动造任务 | 明确不符合 |
| 功能角色 | 角色由节点、时间、功能、竞争和次日任务共同决定 | 角色多为 UNKNOWN；有自然语言功能，但缺角色候选状态和迁移合同 | 未完整实现 |
| 真实竞争组 | 同方向、同起算、同任务、相近功能才比较 | 动作组形式上隔离，但通用任务可能先把不同功能股票放入同一池 | 部分符合 |
| 当前任务 | 先确定节点缺少的关键功能，再找承担者 | Stage C 可在任意 READY 任务中选择，且没有硬门防止按池大小选任务 | 明确不符合 |
| 主/替代/不行动 | 替代路径盘后预定义，可执行且不能临时加票 | 当前只有主任务内 PRIMARY/BACKUP 可执行；跨方向替代通常只有叙述，没有独立动作叶子 | 未完整实现 |
| 竞价/开盘验证 | 只验证盘后计划，不创造新故事 | 条件树和快照纪律基本已实现 | 基本符合 |
| 审计 | 能核对事实、规则、候选池和方法忠实度 | 独立审计有效拦截，但输入缺 facts/task_candidates/candidate_pools | 部分符合 |
| 回评 | 收盘后验证任务、角色和路径 | 本次尚未进入，且计划未通过审计 | 未执行 |

## 四、完整问题清单

### P0：不修就不能重新跑 3 月 2 日

#### P0-1 固定的方向比较顺序

`prompts.py` 把“逐层淘汰”实现成“高度等前项一旦领先，后项一般不能反转”。方案没有这条通用规律。1 月 5 日案例里商业航天胜出，是因为高度、二板、首板和大成交同时完整，不是因为高度单项拥有永久优先权。

应改为：

- 先用明确反证排除孤立单票和无法形成路径的方向；
- 对剩余方向建立多维关系矩阵；
- 一方向只有在关键关系上支配竞争方向，且不存在足以否定它的反证时才成为主路径；
- 各有优劣时输出 `NONE` 或并列待验证，不能用预设字段顺序强排。

#### P0-2 节点被 `task_resolver` 绕过

自动生成最高板、同板级、首板和炸板任务的代码违反“节点识别器只负责打开对应候选生成器”的主轴。

应改为：每个可执行任务必须有合法 `node_id`、节点状态、生成器、起算日、候选范围、确认和取消条件。没有动作节点时只能生成 `OBSERVATION_ONLY`，不能生成 `ACTION_READY`。

#### P0-3 `READY` 只取决于池里有没有股票

当前任务和候选池只要非空就 READY，没有检查：

- 节点是否已合法打开；
- 方向是否允许动作；
- 前态是否足以定义该任务；
- 该任务是否仅为方向验证；
- 必需规则是否存在；
- 数据缺口是否阻断该任务。

需要把状态拆为：

- `ACTION_READY`：节点和任务关系完整，可写盘后动作叶子；
- `OBSERVATION_ONLY`：只能验证方向、角色或层级，不能买；
- `BLOCKED`：关键前态或数据不足；
- `EMPTY_VALID`：节点合法但当天没有候选。

#### P0-4 任务选择可被“候选少、容易收敛”驱动

Stage C 缺少“为何当前节点最需要该任务”的确定性合同。编译器只验证被选任务来自列表，不验证选择理由。

必须禁止以下理由：

- 候选只有一只；
- 候选少于其他任务；
- 更容易输出 SINGLE/PAIR；
- 其他任务太难比较。

无法证明哪个任务更重要时应 `NO_ACTION`，而不是换一个容易收敛的任务。

#### P0-5 被反推与角色替代混淆

应在 Schema 中拆成至少三个状态：

- `INDEPENDENCE_NOT_CONFIRMED`：独立性没有成立；
- `PUSHED_BY_REFERENCE`：被参照物反推；
- `ROLE_REPLACED`：同任务或明确功能迁移后的角色替代已确认。

不同任务、不同层级的验证对象不能直接触发 `ROLE_REPLACED`。

#### P0-6 非相邻账本被当成“昨天”

`load_prev_ledger()` 返回目标日期之前最近的一份账本。3 月 2 日因此读取了 1 月 5 日。

影响不止输入：

- `day_factpack.py` 把 `previous_state_summary.available` 写成 true；
- 商业航天和雷科防务带入 1 月 5 日旧状态；
- `eod_pipeline._load_prev_state()` 把整份旧账本写入 `yesterday_state.json`；
- `save_ledger()` 也会把最近旧账本作为昨天，继续写入 `stage_yesterday` 和 `previous_role`。

必须先由交易日历算出实际上一交易日，只有账本日期严格等于该日期才允许迁移。其他旧账本只能作为 `stale_history_reference`，不能进入 yesterday 状态。

#### P0-7 题材归组表为空

`rules/theme_registry.json` 的 `themes` 为空。系统目前基本按涨停原因原文一对一分组。

这会把可能属于同一资金路径的油服工程、石油化工、油气开采、天然气、燃气等拆成多个方向，也可能把同一产业链的不同标签完全隔离。3 月 2 日商业航天与能源链的比较因此不具备最终可信度。

不能让 AI 临时凭语义合并。应建立有日期版本、证据来源、父方向和细分方向的映射：

```text
raw_reason → canonical_subtheme → parent_direction → evidence → valid_from/to
```

#### P0-8 规则回放版本切片错误

当前 `_available_as_of()` 要求一条规则的所有 `source_dates` 都早于回放日。

结果：

- `ROLE_TOTAL_CORE` 因后来加入 2026-09-05 来源，在 2026-03-02 整条消失；
- `G09_CORE_SWITCH` 因后来加入 2026-03-14 来源，在 2026-03-02 整条消失；
- `G06_DIVERGENCE_REPAIR` 因后来加入 2026-05/06 来源，在 2026-03-02 整条消失。

这些规则早期版本实际已经存在。正确做法不是简单改成“任一来源早于日期就可用”，而是把规则拆成版本：早期版本只含当时已形成的内容，后续帖子形成增强或修订版本，回放时只加载当日可见版本。

### P1：会造成推理失真或审计漏检

#### P1-1 已有事实没有进入 fact catalog

当前 fact catalog 主要登记环境、方向和股票三类事实，没有给以下内容稳定 fact_id：

- 指数上下文；
- 逐方向昨日买方反馈；
- 数据完整度；
- 公告和热点可用性；
- 监管与风险状态；
- 前一交易日状态可用性。

所以 Stage B/C 会引用 `facts.previous_buyer_feedback`、`facts.data_completeness...` 等 JSON 路径，审计无法像普通事实一样核验。

特别是方向事实本身没有附逐方向昨日反馈。模型需要从 75 条全市场记录里自行找同名方向并计数，容易漏算或错算。

#### P1-2 环境历史可用性的硬门过松

`check_stage_b()` 和 `path_comparison_contract` 使用“前日状态账本或昨日买方反馈任一可用”就视作历史确认数据可用。

但不同环境状态需要不同最低证据。只有昨日买方反馈，并不足以确认轮动、切换、主升或退潮完成；只有旧账本，也不足以证明今日买方得到奖励。

需要按状态定义最小证据合同，而不是一个布尔 OR。

#### P1-3 通用任务没有对应 roubing 规则

`HIGHEST_LADDER_CONTINUATION` 不是 G1—G12 中一个可以无条件打开的通用生成器；`BROKEN_BOARD_REPAIR` 也不能因为出现炸板就自动成立。

当前这些任务默认只绑定分离、反后验、禁止打分等工程纪律，没有绑定能证明任务合法的作者规则。尤其炸板修复必须有主流/强分支前态和大分歧节点，或明确的突破后主动分歧前态。

#### P1-4 同一节点会污染不相关任务

任务解析器把主方向的全部 nodes 传给每一个自动任务。若方向有 G8 节点，最高板、首板、炸板等自动任务都可能继承该节点的 method generator，即使该节点只适用于共同首板或同起算竞争。

任务必须一对一引用真正打开自己的节点，不能共享方向级节点列表。

#### P1-5 任务起算日可能脱离节点起算日

`_make_task()` 优先用候选股票自身连续涨停的起点作为任务 anchor，而不是节点 anchor。

3 月 2 日的商业航天观察节点 anchor 是 2026-03-02，最高身位任务却变成 2026-02-26。股票运行起点可以作为 stock start，但不能覆盖 node anchor。应分成：

- `node_anchor_date`；
- `candidate_start_date`；
- `competition_anchor_date`。

#### P1-6 首板晋级与首板扩散重复生成

同一批首板会同时形成 `SAME_LEVEL_PROMOTION` 和 `FIRST_BOARD_DIFFUSION` 两个任务。它们候选相同，只是自然语言名称不同，制造了虚假的多任务选择。

共同首板的动作竞争、方向扩散验证和高位/容量响应应在一个节点合同中分角色表达，而不是重复造两个动作任务。

#### P1-7 跨方向替代路径不可执行

Stage B 虽保存竞争方向，但 `task_resolver` 只为一个 primary theme 生成任务。Stage C 的 `alternative` 只有叙述字段，没有独立 task/pool/action leaf。

因此主方向失败后，系统通常只能不行动，不能执行盘后已经预定义的“若电网设备/能源线独立胜出，则做该方向某任务某股票”。

应允许盘后最多冻结：

- 一个主方向动作分支；
- 一个真正独立的替代方向动作分支；
- 一个不行动分支。

替代分支也必须拥有自己的 node、task、candidate pool、PRIMARY/BACKUP 和独立成立条件，不能盘中临时创建。

#### P1-8 Stage C 一次读取近 1MB，且流程和 Prompt 自相矛盾

Stage C 当前同时接收完整 facts、全部 task candidates 和全部 candidate pools。3 月 2 日任务包中：

- facts 约 760KB；
- candidate pools 约 72KB；
- stage_b 约 116KB；
- 另有 Prompt、Schema 和规则。

Prompt 又要求“先选任务，再只读取该任务池”，但同一次模型调用不可能真正做到先选后裁剪。这既增加 429/超时概率，也增加被其他任务和全市场噪声影响的概率。

应拆成两个模型步骤：

1. Stage C1 只读方向、合法节点和任务摘要，选择任务或不行动；
2. 程序据此裁剪事实；Stage C2 只读所选任务完整池和必要验证对象，生成条件计划。

#### P1-9 独立审计输入不完整

审计当前只收到 stage_b、stage_c、rules 和少量反方原帖，没有收到：

- facts.json；
- task_candidates.json；
- candidate_pools.json；
- compiled_plan.draft.json。

因此审计无法完整核对 fact_id 指向的真实内容，也无法判断任务和候选池合同是否被正确引用。

#### P1-10 原帖反方检索仍是固定关键词 + 10 条

正式 Stage B/C 不读取原帖是正确的；原帖只在独立审计阶段用于找规则误用和反例，这也符合方案。

但当前审计只用固定关键词检索 10 段，未按本次实际 node/task/role 动态构建反方证据包。已有的召回测试主要覆盖 CORE_SWITCH，不能证明所有节点都不会漏召回。

#### P1-11 角色模型缺少“候选状态”

当前正式 role 只能直接写总核心、容量核心等，或写 UNKNOWN。为了不瞎猜，3 月 2 日几乎全部为 UNKNOWN。

方案和执行手册实际需要表达：

- 高度核心候选；
- 容量观察；
- 扩散/助攻候选；
- 当前功能已观察，但角色尚未确认。

应将 `role_name` 和 `role_status=CANDIDATE|PROVISIONAL|CONFIRMED|REPLACED|CANCELLED|UNKNOWN` 分开，避免自然语言承担正式状态机。

#### P1-12 PRIMARY/BACKUP 静态排序合同可能把案例偏好泛化

两只股票证据冲突时，当前 `_pair_preference_contract()` 固定把“开盘展示 + 成交分位”共同领先者设为 PRIMARY，把“封板效率 + 封单承载”共同领先者设为 BACKUP。

这来自 1 月 5 日案例的工程收敛，不是已证明适用于所有节点、所有角色的 roubing 通用优先级。由于 BACKUP 只有 PRIMARY 直接失败才能接管，这个排序会实质影响动作结果。

需要按节点和角色任务决定证据家庭，或在证据冲突时保留真正对称的条件分支，不能把一个案例的优先验证顺序写成全局合同。

### P2：工程治理问题

#### P2-1 存在两套候选生成架构

项目同时保留：

- `candidates/generators.py`：节点生成器架构；
- `reasoning/task_resolver.py`：方向结构自动任务架构。

正式 EOD 只使用后者，但大量测试仍测试前者。两套架构的规则边界不一致，容易出现“测试通过的是一套，生产运行的是另一套”。

#### P2-2 测试把错误行为当成正确行为

`tests/test_reasoning_chain.py` 明确构造 `generator=None` 的观察节点，然后断言仍应生成商业航天分支任务。这正好把“无节点也能生成任务”的偏差固化成了回归测试。

当前测试主要证明：

- 字段齐全；
- 股票来自某个池；
- 没有写死商业航天代码；
- PRIMARY/BACKUP 数量合法。

它们没有证明：

- 任务确实由合法节点打开；
- 主路径没有被单一高度决定；
- 任务不是因候选少而被选择；
- 不同角色没有被写成替代。

#### P2-3 TODO 和优化报告把“结构完成”写成“逻辑完成”

`IMPLEMENTATION_TODO.md` 的 B1、B7、B8、N2、N3、N4、N5 已标完成；优化报告也声称任务由主路径关系和方法节点生成。

实际情况是 Schema、结构校验和特定 fixture 已完成，但方法语义没有完成。应将这些项目重新打开，并把验收标准改成真实反例测试，而不是仅检查 JSON 结构。

#### P2-4 旧角色池还有“最旧记录优先”的潜在问题

`prior_role_pool()` 按日期从旧到新遍历账本，`_dedupe()` 遇到重复股票时保留第一次记录，没有用最新相邻角色覆盖。虽然当前 EOD 主链暂未使用这套旧生成器，但一旦重新启用，会把更旧角色当成当前角色。

## 五、方案本身是否有问题

方案主轴没有错，问题主要出在实现时把两个局部概念过度泛化：

1. 执行手册 1 月 5 日案例中的“逐层淘汰”，被写成了所有日期通用的固定字段优先级；
2. 为了把 1 月 5 日收敛到北斗星通/烽火通信，新增的动态任务和 PRIMARY/BACKUP 合同，被泛化为所有方向的通用任务解析算法。

方案文档也应补一条消歧义说明：

> 逐层淘汰只表示先排除明确不满足路径基本条件的对象，再在真实可比对象之间比较关系；不表示“高度、板级、首板、成交、反馈”构成固定词典序，也不表示前一项领先后后续反证不能推翻。

## 六、正确修复顺序

必须按以下顺序修，不能先调 Prompt、后补测试：

1. 冻结当前错误产物；继续保持 3 月 3 日数据隐藏。
2. 修复实际上一交易日识别，并同时修复 factpack、model package、save_ledger 和 prior role 读取。
3. 建立带父子层级和生效日期的题材归组；重新生成 3 月 2 日方向事实。
4. 给指数、逐方向昨日反馈、数据完整度、上下文和风险状态生成稳定 fact_id。
5. 将规则库拆成可回放版本，修复 3 月 2 日缺早期规则的问题。
6. 重写 Stage B 方向比较：关系矩阵 + 反证 + 支配/不可判，不允许固定高度词典序。
7. 删除正式主链中“看到板位就自动造任务”的逻辑；所有 ACTION_READY 必须由合法节点打开。
8. 把节点观察、方向验证和股票动作分成不同状态，不再用非空池等同 READY。
9. 拆分 Stage C1 任务选择与 Stage C2 股票计划；禁止按池大小和易收敛度选择任务。
10. 拆分被反推、独立性未确认和角色替代；角色替代只允许同任务或明确功能迁移。
11. 为跨方向替代建立独立、盘后冻结的 node/task/pool/action branch。
12. 给 Stage B、task resolver、fidelity 和 compiler 增加语义硬门。
13. 审计加入 facts、task candidates、candidate pools 和 compiled plan，并按实际节点动态召回反方规则。
14. 删除或统一旧候选生成链，重写固化错误行为的测试。
15. 全量测试通过后，只重新跑 2026-03-02 盘后；审计通过并冻结正式计划后，才恢复 2026-03-03 数据。

## 七、重新跑 3 月 2 日前的强制验收条件

以下条件缺一不可：

- 商业航天不能因为“有 3 板”自动胜过电网设备；
- 电网设备昨日 4/4 正反馈必须直接存在于该方向事实中，而不是让模型从 75 行里自己数；
- 能源相关方向在完成证据化父子归组前，主路径结论必须标记题材边界风险；
- 商业航天 `generator=null` 时，最高板、首板和炸板任务不得为 ACTION_READY；
- 任一 execution task 必须能追溯到唯一 node_id、合法 generator 和对应规则版本；
- “候选少、容易 SINGLE/PAIR”必须被程序直接判为非法任务选择理由；
- 低位扩散先强只能证伪高位独立性，不能自动写角色替代；
- 非相邻 1 月 5 日账本不得以任何 yesterday/previous role 字段进入 3 月 2 日事实或新账本；
- 审计能直接读取并核验 facts、tasks、pools 和 compiled plan；
- 如果没有合法动作节点，正式结果必须是 `NO_ACTION`，不能为了给股票而创建任务。

在这些条件满足前，当前 `runs/20260302/run_status.json = BLOCKED_AUDIT` 是正确状态，现有“商业航天—泰嘉股份”只能作为失败样本，不能作为正式盘后预案。
