# roubing_engine 推演架构优化执行报告

日期：2026-09-13

## 一、这次真正改成了什么

旧链路的核心问题是：Stage B 给方向和节点，Stage C 从较大的候选池写很多股票作战卡，Stage D 再验证全部候选。这样即使每段文字看起来合理，系统仍然回答不了“明天符合什么情况买哪一只，否则放弃”。

新链路是：

```text
结构化事实与事实ID
→ 市场环境
→ 全部方向逐一比较
→ 动态主路径（最多一个，也允许没有）
→ 方法节点
→ 动态执行任务池
→ 选择明天唯一要解决的任务
→ 股票功能拆分
→ 同任务竞争
→ 最多两个动作叶子
→ 09:25封闭条件树收敛唯一对象
→ 09:35只验证这个对象
→ BUY或NO_ACTION
```

生产代码没有写死商业航天、北斗星通、烽火通信或对应股票代码。上述名称仅存在于 `tests/fixtures/20260105/` 黄金案例中。

## 二、每个阶段实际看什么、怎么分析、输出什么

### 1. 事实层

输入：当日涨停、炸板、跌停事件，日线，竞价，开盘撮合，公告/题材可用性，指数上下文，监管与风险数据，前序账本。

新增输出：

- `environment_facts`：市场宽度、梯队、昨日强势买方反馈、上下文数据状态；
- `direction_state_facts`：每个方向的涨停/炸板/跌停、梯队、最高板、首板数、成交、竞价覆盖、扩散数据缺口；
- 个股事实：主方向、细分方向、板级、封板时间、成交分位、前高、是否突破前高、前序角色与任务；
- `fact_catalog`：市场、方向、股票全部事实的稳定 `fact_id`。

硬约束：Stage B/C 的事实性结论必须引用真实存在的 `fact_id`；引用不存在、层级不匹配、方向未引用自身事实、候选未引用自身股票事实都会被程序拦截。

2026-01-05 实际事实包自检：52 个方向、139 个股票观察对象、207 个事实目录项。商业航天事实为 14 家涨停、5 家炸板，梯队为 4板1家、2板4家、首板9家；人脑工程为 27 家首板、2 家炸板。这里只是事实，不在事实层判断谁是主线。

### 2. Stage B：环境、全部方向和动态主路径

Stage B 必须：

1. 先判断整体环境；
2. 对 `direction_state_facts` 的每个方向逐一输出评价，不允许只输出最后选中的方向；
3. 分开记录已观察事实、AI推断、反证和数据缺口；
4. 比较方向的延续结构、梯队、失败反馈、买方反馈和内部响应；
5. 选出至多一个 `primary_path`，无法排除竞争方向时输出 `NONE`，数据不足时输出 `BLOCKED_DATA`；
6. 主路径之后才识别 G1—G12 方法节点。

程序硬检：方向不能遗漏或重复；方向名称与事实 ID 必须一致；选中的主路径必须与唯一 `PRIMARY` 方向一致；节点必须引用触发事实；单票节点不能引用事实观察集之外的股票。

### 3. 动态执行任务层

新增 `reasoning/task_resolver.py`。它不选最终股票，只根据当天被选中的主路径、股票关系和方法节点生成当日任务候选：

- 最高身位延续；
- 同板级晋级；
- 同细分分支延续；
- 容量承载验证；
- 首板扩散；
- 炸板修复；
- 核心切换；
- 补涨；
- 二波伴生。

所有任务的方向、股票和验证对象只能来自当天事实包。没有主路径时不生成股票任务；方法节点缺明确候选时输出 `BLOCKED_DATA`，不会扩大范围找股票。

2026-01-05 黄金案例中，“卫星导航分支二板主动延续”不是写死结果，而是由以下结构自动生成：同一细分方向有一个更高板级验证对象雷科防务，同时有两只同为二板、承担相同延续任务的北斗星通和烽火通信。因此雷科防务是验证对象，另外两只是同任务竞争者。

反向测试把主路径换成人脑工程后，任务池只使用人脑工程自身事实，没有混入任何商业航天股票。

### 4. Stage C：功能拆分、同任务竞争和盘后预案

Stage C 不再面对全市场自由选股，而是：

1. 接受 Stage B 主路径；
2. 说明该路径明天缺什么功能；
3. 从动态任务池选择一个 `execution_task`，或明确不行动；
4. 只读取该任务的候选池；
5. 把股票分为 `ACTION_COMPETITOR`、`VALIDATION_ONLY`、`NOT_RELEVANT`；
6. 只比较承担相同任务的动作竞争者；
7. 输出 `SINGLE`、`CONDITIONAL_PAIR` 或 `NO_ACTION`。

动作叶子最多两只。三只以上仍无法排除时，系统要求 `NO_ACTION`，不允许留给盘中临时挑选。验证对象只能确认方向或分支是否响应，永远不能进入动作名单。

PRIMARY 和 BACKUP 必须分别写清自己的任务、竞价条件、开盘条件、降级条件和直接失败条件。条件必须描述相对自身任务、对手和方向的关系，不能使用固定高开百分比、固定封单额或固定成交额。

### 5. 计划编译器

新增 `reasoning/plan_compiler.py`。AI 的 Stage C JSON 只有经过该编译器才会生成正式 `executable_plan.json`。

编译器检查：

- 主路径、执行任务和股票方向一致；
- 任务来自冻结任务池；
- 动作股票来自同一任务竞争组；
- 验证对象不能买；
- 最多两个动作叶子；
- 意外强票不能加入；
- 固定数字阈值直接判废；
- PRIMARY/BACKUP 都有独立任务；
- BACKUP 只能在 PRIMARY `DIRECT_FAIL` 且自身独立达标时接管；
- PRIMARY 只是 `DOWNGRADED` 或 `NEEDS_OPEN_VALIDATION` 时不能切换。

任何一项不通过，运行状态为 `BLOCKED_PLAN_COMPILER`，不会保存正式计划。

### 6. Stage D：09:25 和 09:35 封闭条件树

新增 `reasoning/condition_tree.py`，验证事实拆为：

- `action_leaf_facts`：本快照允许判断的动作叶子；
- `validation_context_facts`：只用于验证方向/分支响应；
- `prior_auction_result`：09:35 必须继承的 09:25 结果。

09:25 状态：

- `MEETS_AUCTION_TASK`
- `NEEDS_OPEN_VALIDATION`
- `DOWNGRADED`
- `DIRECT_FAIL`
- `DATA_INSUFFICIENT`

09:25 只能输出一个进入 09:35 的对象或停止，不能直接买入。PRIMARY 降级时停止；只有 PRIMARY 直接失败且 BACKUP 独立满足时才切换。验证对象否定任务或验证数据不足时，也不能保留动作对象。

09:35 只接收 09:25 唯一对象。完成开盘任务且验证对象确认时才可 `BUY`；失败为 `NO_ACTION`；分钟线或必要验证事实不足为 `DATA_INSUFFICIENT`。09:25 没对象时，09:35 不得重新找票。

程序还核对股票代码、`leaf_id`、PRIMARY/BACKUP 层级和验证对象完整覆盖，防止模型换计划叶子。

### 7. 原帖使用边界

正式 Stage B/C/D 已彻底移除 `original_posts.md`。正式每日推理只读取结构化事实、离线编译规则、状态账本、动态任务池、候选池和 Schema。

原帖只在独立审计阶段作为离线反例材料使用，用于发现规则误用和个案泛化。任务目录复用时会主动删除旧的原帖和案例覆盖文件，避免残留输入污染新推理。

### 8. 子进程隔离和失败状态

runner 禁止加载外部投资 skill，并关闭插件、应用和 skill 搜索。隔离环境只保留 Codex 认证以及 cc-switch 当前选中的模型/provider 配置；MCP、插件、用户规则、skills、hooks 和通知均不继承。

正式运行开始时先把状态写成 `RUNNING`。Stage B/C 模型启动失败会写 `BLOCKED_MODEL_STAGE_B/C`，并记录 stdout/stderr 日志路径。旧 `APPROVED` 状态不会残留，因此 Stage D 无法误用旧计划。

## 三、2026-01-05 / 2026-01-06 黄金回放结果

黄金 fixture 只用于验证通用代码是否复现已经人工确认的推演关系，不是生产规则。

盘后冻结：

- 主路径：商业航天；
- 执行任务：卫星导航细分的同板级主动延续；
- PRIMARY：北斗星通；
- BACKUP：烽火通信；
- 验证对象：雷科防务。

2026-01-06 实际竞价数据：

- 北斗星通竞价过程 54.90 → 49.60，09:25 撮合 49.98，约 +0.02%；
- 烽火通信竞价过程 38.82 → 36.00，09:25 撮合 35.98，约 +1.96%。

条件树结果：

- 北斗星通为 `DOWNGRADED`，没有完成盘后要求的主动延续，但该状态不是 `DIRECT_FAIL`；
- 烽火通信为 `NEEDS_OPEN_VALIDATION`，仅说明自身还可观察，不代表已独立接管；
- 因 PRIMARY 只是降级，不能切 BACKUP；
- `current_action_candidate = null`；
- 09:25 结果为 `NO_ACTION / STOP`；
- 09:35 不允许重新选烽火通信或其他意外强票，继续 `NO_ACTION`。

## 四、自检结果

2026-09-14 最新本地等价检查结果：122 项测试全部通过，`compileall` 通过，282 篇规则语料和案例卡数量检查通过。

覆盖的关键反例包括：

- 非商业航天主路径不会混入商业航天股票；
- 三只以上未解决动作对象必须不行动；
- 固定高开百分比被拒绝；
- 验证对象不能成为动作叶子；
- PRIMARY 降级不能切 BACKUP；
- PRIMARY 直接失败且 BACKUP 独立满足时才可切换；
- 盘后未选中的意外强票不能加入；
- 09:35 只能接收 09:25 唯一对象；
- 验证对象否定任务时不能保留动作对象；
- 正式 Stage B/C 任务目录不存在原帖输入；
- 生产 Python 不含黄金案例题材、股票名或代码。

## 五、真实回归与剩余边界

真实 2026-01-05 EOD → 2026-01-06 09:25 → 09:35 已完成，EOD 和两次 Stage D 独立审计均有正式产物。2026-03-02 盘后真实回归最终 `APPROVED`，但没有形成主路径和动作任务，真实结论是 `NO_ACTION`。

仍不宣称策略盈利或具备统计有效性。多日历史角色迁移、历史盘口完整恢复、长期 walk-forward 和收益评估仍属于后续历史工程，不在本次单日方案对齐结论内。

2026-09-14 追加修复：正式结果仍保留 `_provenance.generated_at` 供审计，但运行时间不再进入 C1、C2、EOD audit 和 Stage D audit 的模型输入；同时新增 G8 起算日当天不得 `ACTION_READY` 的硬门。盘后 `NO_ACTION` 时是否跳过 Stage D 按用户决定保持原行为，不做修改。
