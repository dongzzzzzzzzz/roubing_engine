# roubing_engine 完整修复 TODO

更新时间：2026-09-14

执行纪律：严格按编号顺序处理。每项完成后必须先运行对应自检；自检通过后才能进入下一项。`DONE` 只表示代码、测试和实际回归均通过，不表示策略已经盈利。

## 本次范围说明

本次只修复“2026-01-05 单日盘后推理 + 2026-01-06 竞价/开盘验证工程”。以下历史连续推演事项按用户要求暂缓，不作为本轮阻塞：

- 2026-01-05 之前的历史状态迁移；
- 前一交易日买方溢价反馈；
- 被排除股票的历史次日动态竞争回放；
- 早期历史分钟线/逐笔数据恢复；
- 多日 walk-forward、收盘结果回评和收益统计；
- 长期历史 universe 覆盖扩充。

## 串行修复清单

### A. 数据接线与事实包

- [x] A1 修复 `opening_match` 字段兼容：`price` 作为 09:25 撮合价，补 T 日前收盘价并计算开盘涨跌幅。自检：单元测试、真实 2026-01-06 数据检查通过。
- [x] A2 增加 opening-match 的字段、行数、代码覆盖回归测试，防止字段接错后静默变成 null。自检：真实分区字段、行数、代码唯一覆盖及全量测试通过。
- [x] A3 将竞价未匹配量、未匹配方向和有符号未匹配量纳入事实摘要；明确不能据此断言撤单者。自检：摘要单测、全量测试和 2026-01-06 9 只候选真实摘要通过。
- [x] A4 构建当日指数/板块事实接口：能取到就写入截至快照数据，取不到就写结构化 `BLOCKED_DATA`，不得伪造。自检：缺失分区阻断、临时分区按 09:25/09:35 截断、全量测试通过。
- [x] A5 构建方向事实摘要：涨停、炸板、跌停、梯队、竞价覆盖、成交额和可观察扩散分别记录；非涨停扩散无成员映射时必须明确不可用。自检：2026-01-05 人脑工程/商业航天方向事实计数、覆盖和扩散阻断通过。
- [x] A6 核验公告/F10 题材采集链路：区分“采集器存在”“当日实际有数据”“历史可用”，并把状态进入事实包。自检：事实包 context_data 状态和全量测试通过。
- [x] A7 录入并校验官方监管规则版本；按生效日期选择；没有核验文本时保持 `BLOCKED_DATA`。自检：沪深 2023 规则按 2026-01-05 生效日期选取，未核验阈值仍阻断距离计算。
- [x] A8 增加停复牌、重点监控、风险提示数据接口；取不到时结构化标记，不由 AI 推断。自检：三类风险数据缺失均输出 BLOCKED_DATA，事实包可见。

### B. 主路径、动态任务、候选和角色

- [x] B1 取消“为主流方向补通用生成器”的旧方案；Stage B 必须逐一比较事实包全部方向并输出动态 `primary_path`，允许无主路径或数据阻断。自检：Schema、全方向覆盖和事实 ID 硬校验通过。
- [x] B2 建立正式角色族：核心、容量、补涨、伴飞、二波伴生、旧核心残余、跟风、UNKNOWN。自检：Schema 和角色映射测试通过。
- [x] B3 保留 roubing 特定细分角色，并增加 `role_family` 归属；角色族不能替代作者案例中的 A/B/C/D 字母。自检：细分角色到角色族映射已硬校验。
- [x] B4 维持新股票 A/B/C/D 为 `UNKNOWN`；只有原帖明确对应时才能引用作者字母。自检：Schema 继续只允许 `UNKNOWN`。
- [x] B5 为容量角色建立事实任务字段：成交后推进、回踩收回、板块带动、重心变化、被替代状态；不设置统一成交额阈值。自检：Stage C 候选 Schema 已强制五项任务字段。
- [x] B6 为主动/被动建立事件模型：参照物、事件顺序、目标行为、数据充分性、结论状态；无完整委托流只能 `UNKNOWN`。自检：事件模型字段和 UNKNOWN 状态已强制进入 Schema。
- [x] B7 为唯一性建立完整竞争组状态：全组覆盖率、正负样本、次日任务、替代事件和唯一性状态；不能按单日涨幅/封单宣布唯一。自检：竞争组强制覆盖状态、次日任务和唯一性状态。
- [x] B8 增加 Stage C 逐字段事实验证器，检查角色、节点、起算日、对手、排除理由和证据是否与候选池事实一致。自检：候选池方向不一致测试通过。

### C. Stage D 验证与审计

- [x] C1 统一 Stage D 提示词、Schema、示例和代码枚举，包含 `DATA_INSUFFICIENT`。自检：提示词枚举测试和全量 Schema 测试通过。
- [x] C2 增加 Stage D 事实一致性硬门：缺开盘、分钟、逐笔、板块或竞争组数据时禁止输出相应确认结论。自检：缺失开盘五分钟/09:25 越界单测和全量测试通过。
- [x] C3 增加 Stage D 独立审计文件和审计规则，检查后验、缺失数据、主动/被动、共振和唯一性越界。自检：独立审计任务、审计结果文件和失败阻断路径已接入。
- [x] C4 修复 09:25 临时 jq 自检，改为稳定 Python 校验；自检失败必须阻断正式结果。自检：验证流程统一使用 Python 硬门，仓库业务代码无 jq 自检调用。
- [x] C5 增加 09:25/09:35 的快照边界、字段覆盖和结论状态回归测试。自检：22 项边界/字段相关测试和全量测试通过。

### D. 原帖规则和证据追踪

- [x] D1 将 282 篇全文编译成可追踪规则单元和案例卡：来源日期、标题、哈希、原文片段、适用条件、反例、证据等级。自检：`corpus_units.json`/`corpus_case_cards.json` 均为 282 篇、哈希唯一、全文保留。
- [x] D2 每个节点预定义必需规则/案例集合，用于离线规则整理和独立审计；正式每日 Stage B/C/D 不读取原帖。自检：生产任务包只含事实、规则、状态、任务池、候选池和 Schema。
- [x] D3 删除正式任务包里的 `original_posts.md` 与案例检索覆盖输入；独立 audit 可读取离线反方原帖，但不得把个案搬回当日推理。自检：复用旧 run 目录时也会主动清除残留原帖文件。
- [x] D4 增加规则检索召回测试，防止核心案例被关键词漏检。自检：CORE_SWITCH 必需规则与案例哈希召回测试通过。

### E. 运行环境和工程质量

- [x] E1 runner 禁止并检测外部投资 skill；发现 `capital-destination-reasoner`、`serenity-skill` 等污染时自动标记 `CONTAMINATED` 并阻断。自检：污染标记检测测试通过。
- [x] E2 保存子进程 stdout/stderr 和环境摘要，区分业务错误、插件警告和 skill 污染。自检：runner 已写入日志/环境摘要/contamination.json 分支，全量测试通过。
- [x] E3 为 Stage B/C/D/audit 增加连续真实模型回归入口；每阶段失败不能继续写正式账本。自检：`roubing_engine.reasoning.regression` 串行入口及失败短路测试通过。
- [x] E4 增加当前范围内的完整单日回归测试：事实包 → Stage B → 候选池 → Stage C → fidelity → audit → Stage D。真实 2026-01-05/06 全链路已完成；2026-03-02 盘后真实模型回归最终 `APPROVED`，结论为 `NO_ACTION`。
- [x] E5 增加 CI 配置和本地等价检查：单元测试、compileall、Schema、快照边界、污染检测。自检：`scripts/check_local.py` 和 GitHub Actions 配置均通过本地等价检查。

## 暂缓但保留的历史事项

- [ ] H1 多日历史状态和角色迁移回放。
- [ ] H2 历史分钟线、逐笔盘口和严格开盘回放。
- [ ] H3 收盘 outcome 与预测质量评估。
- [ ] H4 长期逐日 universe 补采和 walk-forward 收益统计。

## 2026-09-13 推演架构升级

- [x] N1 结构化事实：市场、方向、股票生成稳定 `fact_id`，加入环境事实、方向状态事实、细分方向、板级、封板、成交分位、前高和前序角色/任务。自检：2026-01-05 事实包 207 个目录项稳定复现。
- [x] N2 Stage B：强制输出环境推演、全部方向比较、至多一个动态主路径、竞争方向、反证和方法节点；所有事实性结论必须引用真实 `fact_id`。自检：Schema 和 fidelity 硬门通过。
- [x] N3 动态执行任务：新增 `task_resolver.py`，从当日主路径关系生成最高身位延续、同板级晋级、同细分延续、容量验证、首板扩散、炸板修复、核心切换、补涨、二波伴生任务。自检：非商业航天反向测试不会混入商业航天股票。
- [x] N4 股票功能与同任务竞争：Stage C 先选唯一任务，再将股票标为 `ACTION_COMPETITOR`、`VALIDATION_ONLY`、`NOT_RELEVANT`，只比较同任务对象。自检：Schema 与冻结任务池覆盖检查通过。
- [x] N5 计划编译：新增 `plan_compiler.py`；动作叶子最多两只，三只以上未解决必须不行动，验证对象不能买，具体题材/股票只能来自事实包，固定高开/成交阈值直接判废。自检：专项测试通过。
- [x] N6 封闭条件树：新增 `condition_tree.py`；09:25 只能收敛一个 09:35 对象，PRIMARY 降级不得切 BACKUP，只有 PRIMARY 直接失败且 BACKUP 独立满足才可切，09:35 不得重新找票。自检：状态机专项测试通过。
- [x] N7 2026-01-05 黄金案例：盘后动作叶子为北斗星通/烽火通信，高位验证对象为雷科防务；2026-01-06 实际竞价验证为 PRIMARY 降级、BACKUP 仅待开盘验证，按规则最终 `NO_ACTION`。这些名称只存在测试 fixture，不存在生产 Python。自检：真实竞价字段和封闭状态机测试通过。
- [x] N8 正式推理移除原帖：Stage B/C/D 只用编译规则，不按当天关键词检索帖子。自检：任务包残留清理和测试通过。
- [x] N9 使用真实模型重新跑完整 2026-01-05 EOD → 2026-01-06 09:25 → 09:35，保存新架构结果。EOD 审计通过，09:25 和 09:35 均已生成正式结果与独立审计文件，最终为 `NO_ACTION`。

## 2026-09-14 最终方案对齐复核

- [x] F1 下游模型输入移除 `_provenance.generated_at` 等运行时元数据。正式结果文件继续保留生成时间用于审计，但 C1、C2、EOD audit 和 Stage D audit 不再看到墙上时钟日期，避免把 2026-09-14 误认成历史回放市场日期。
- [x] F2 新增 G8 起算日硬门：共同首板或同日起步的当天只能建立唯一性观察组，不得标为 `ACTION_READY`，不得打开次日动作任务。
- [x] F3 修正审计失败测试夹具，使其完整引用冻结的 `no_action_rule_ids`；确认新 C1 规则合同生效后，测试才能真正进入独立审计失败分支。
- [x] F4 用最新代码重新确定性校验 2026-03-02 正式结果：Stage B 零违规、Stage C fidelity `PASS`、计划编译 `PASS`，结论仍为 `NO_ACTION`。
- [x] F5 最新本地全量检查：122 项测试全部通过，compileall、Schema、282 篇语料数量检查全部通过。
- [x] F6 盘后 `NO_ACTION` 时自动跳过 Stage D：经用户明确决定“不改”，相关修改已完整回退，现有 `run_single_day` 与手工 Stage D 行为保持原样；本项以“不实施”关闭。

## 验收要求

每个 A/B/C/D/E 项目完成后必须：

1. 运行新增或对应的最小单元测试；
2. 运行受影响的完整测试集；
3. 检查实际生成的 JSON/Parquet 字段和覆盖率；
4. 在通过前不得开始下一项；
5. 最终重跑 2026-01-05 全链路并保存修复前后差异。
